import { TransitionRotateDirective } from './transition-rotate.directive';

describe('TransitionRotateDirective', () => {
  it('should create an instance', () => {
    const directive = new TransitionRotateDirective();
    expect(directive).toBeTruthy();
  });
});
